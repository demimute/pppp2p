# NEXT_STEP

## 当前目标
在主线闭环已打通的基础上，继续做桌面产物回归，确认 Electron 打包产物中的目录选择、分析、对比、移除、撤销链路与当前浏览器态 E2E 结果一致。

## 当前事实
- 四条核心 Playwright 回归已通过：`workflow.spec.js`、`compare-shortcuts.spec.js`、`multi-group-history.spec.js`、`repeated-undo.spec.js`
- 当前已有功能的稳定性问题已完成一轮集中修复，包括 Winner 不可移除、快捷键焦点一致性、多组撤销断言、重复撤销夹具污染、rebuild folder 后 stale undo/history 状态、并行 worker 共享夹具目录
- 现阶段最有价值的下一步不是继续补浏览器态测试，而是验证 Electron/打包产物是否与 E2E 结论一致

## 预计动作
1. 启动 `npm run dev:electron`，手动复验目录选择、扫描、分析、对比、移除、撤销
2. 如有需要，对照 `dist-electron/` 产物做一次桌面端冒烟验证
3. 若桌面态暴露新问题，优先修复真实产品问题，再补回归测试
4. 每完成一个关键验证点立刻回写 `ops/state.json` 与项目状态文件
5. 避免再回到泛化治理动作，保持“先验证真实链路，再记状态”

## 验证方法
- `npm run dev:electron` 可启动并稳定连接后端
- 至少完成一次桌面端的“选目录 -> 分析 -> 对比 -> 移除 -> 撤销”闭环
- 如使用打包产物，需确认关键按钮、对比视图、确认弹窗、历史栏在桌面态行为一致

## 完成条件
- Electron 桌面态主线闭环完成复验
- 若打包产物可直接验证，则完成至少一轮桌面端冒烟
- 新发现问题被记录并进入修复/回归闭环
